<section class="section nerux" id="neruxai" aria-labelledby="nerux-title" data-track-section="about_neruxai">
  <div class="container">
    <div class="nerux-intro">
      <span class="section-kicker">La idea que lo conecta todo</span>
      <h2 class="section-title" id="nerux-title">Virtus es el primer paso. NeruxAI es el camino.</h2>
      <p class="section-lede">NeruxAI existe para ayudarte a cuidar tu salud física, mental y espiritual con tecnología que te entiende de verdad. Virtus es donde empieza: conectando lo que haces cada día con quién quieres ser.</p>
    </div>

    <div class="pillars">
      <article class="pillar">
        <div class="pillar-icon" aria-hidden="true">
          <svg viewBox="0 0 32 32" fill="none">
            <path d="M9 20c1.5-6 4.5-9 10-10M18 6l2 4-3 3" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"></path>
            <path d="M9 24c3 2 8 2.5 12-.5" stroke="currentColor" stroke-width="1.6" stroke-linecap="round"></path>
            <circle cx="8.5" cy="20.5" r="2.5" fill="currentColor"></circle>
          </svg>
        </div>
        <span class="pillar-number">01</span>
        <h3>Física</h3>
        <p>Tu energía, descanso y movimiento dan contexto al plan de hoy.</p>
        <small>Check-in corporal</small>
      </article>

      <article class="pillar">
        <div class="pillar-icon" aria-hidden="true">
          <svg viewBox="0 0 32 32" fill="none">
            <path d="M5 18c4-7 7 7 11 0s7 7 11 0" stroke="currentColor" stroke-width="1.7" stroke-linecap="round"></path>
            <path d="M7 11c3-4 6 4 9 0s6 4 9 0" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" opacity=".55"></path>
          </svg>
        </div>
        <span class="pillar-number">02</span>
        <h3>Mental</h3>
        <p>Tu mentor convierte el bloqueo en claridad y la claridad en acción.</p>
        <small>Mentor y tareas</small>
      </article>

      <article class="pillar">
        <div class="pillar-icon" aria-hidden="true">
          <svg viewBox="0 0 32 32" fill="none">
            <path d="M16 5v22M8 13c5 0 8 3 8 8M24 9c-5 0-8 3-8 8" stroke="currentColor" stroke-width="1.6" stroke-linecap="round"></path>
            <path d="M8 13c0-4 2-6 6-6 0 4-2 6-6 6ZM24 9c0-3-1.5-4.5-4.5-4.5C19.5 7.5 21 9 24 9Z" fill="currentColor"></path>
          </svg>
        </div>
        <span class="pillar-number">03</span>
        <h3>Espiritual</h3>
        <p>Tu forma de crecer respeta los valores y la mirada que eliges.</p>
        <small>Perfiles de mentor</small>
      </article>
    </div>
  </div>
</section>

<style>
  .nerux {
    overflow: hidden;
    background:
      radial-gradient(ellipse at 4% 90%, color-mix(in srgb, var(--sage) 20%, transparent), transparent 30rem),
      radial-gradient(ellipse at 94% 10%, color-mix(in srgb, var(--flow) 14%, transparent), transparent 32rem),
      var(--surface-strong);
    color: var(--text);
  }

  .nerux::before {
    position: absolute;
    top: 4rem;
    right: -8rem;
    width: 22rem;
    height: 16rem;
    border-radius: 68% 32% 56% 44% / 40% 63% 37% 60%;
    background: color-mix(in srgb, var(--flow) 11%, transparent);
    content: '';
    transform: rotate(-9deg);
  }

  .nerux-intro {
    position: relative;
    z-index: 1;
    display: grid;
    gap: 1rem 5rem;
    align-items: end;
    margin-bottom: 3.5rem;
  }

  .nerux .section-title {
    max-width: 19ch;
  }

  .nerux .section-lede {
    color: var(--text-muted);
  }

  .pillars {
    position: relative;
    z-index: 1;
    display: grid;
    gap: 0.8rem;
  }

  .pillar {
    position: relative;
    min-height: 19rem;
    overflow: hidden;
    border: 1px solid var(--line);
    border-radius: var(--radius-md);
    padding: 1.6rem;
    background: color-mix(in srgb, var(--surface) 88%, transparent);
    box-shadow: var(--shadow-sm);
  }

  .pillar::after {
    position: absolute;
    right: -2.6rem;
    bottom: -3.4rem;
    width: 8rem;
    height: 7rem;
    border-radius: 70% 30% 46% 54% / 42% 66% 34% 58%;
    background: color-mix(in srgb, var(--pillar-accent, var(--flow)) 9%, transparent);
    content: '';
    transform: rotate(18deg);
  }

  .pillar:nth-child(2) { --pillar-accent: var(--flow); }
  .pillar:nth-child(3) { --pillar-accent: var(--root); }

  .pillar-icon {
    display: grid;
    width: 3.3rem;
    height: 3.3rem;
    place-items: center;
    margin-bottom: 2rem;
    border: 1px solid color-mix(in srgb, var(--root) 22%, transparent);
    border-radius: 65% 35% 58% 42% / 38% 62% 38% 62%;
    background: color-mix(in srgb, var(--sage) 13%, var(--surface));
    color: var(--root);
  }

  .pillar-icon svg {
    width: 1.9rem;
    height: 1.9rem;
  }

  .pillar-number {
    position: absolute;
    top: 1.7rem;
    right: 1.7rem;
    color: var(--text-muted);
    font-family: var(--font-body);
    font-size: 0.68rem;
    font-variant-numeric: tabular-nums;
  }

  .pillar h3 {
    margin-bottom: 0.55rem;
    font-family: var(--font-display);
    font-size: var(--step-2);
    font-weight: 500;
    font-style: italic;
  }

  .pillar p {
    max-width: 22rem;
    margin-bottom: 1.4rem;
    color: var(--text-muted);
    font-size: 0.9rem;
  }

  .pillar small {
    color: var(--root);
    font-family: var(--font-body);
    font-size: 0.68rem;
    font-weight: 700;
  }

  @media (min-width: 54rem) {
    .nerux-intro {
      grid-template-columns: minmax(0, 1.05fr) minmax(20rem, 0.75fr);
    }

    .pillars {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .pillar {
      padding: 2rem;
    }
  }
</style>
