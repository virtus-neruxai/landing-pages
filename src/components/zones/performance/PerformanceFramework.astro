<section
  class="section performance-framework"
  id="norte-renuncia-movimiento"
  aria-labelledby="performance-framework-title"
  data-track-section="performance_framework"
>
  <div class="container">
    <div class="performance-framework__intro">
      <span class="section-kicker">Norte · Renuncia · Movimiento</span>
      <h2 class="section-title" id="performance-framework-title">La dirección también exige renunciar</h2>
      <p class="section-lede">
        Virtus no añade objetivos a una vida ya llena. Te ayuda a decidir qué mantener, qué terminar y qué merece empezar.
      </p>
    </div>

    <ol class="framework-stages">
      <li>
        <span class="stage-number" aria-hidden="true">01</span>
        <div>
          <h3>Norte</h3>
          <p>Qué etapa construyes.</p>
        </div>
      </li>
      <li>
        <span class="stage-number" aria-hidden="true">02</span>
        <div>
          <h3>Renuncia</h3>
          <p>Deja de proteger lo secundario.</p>
        </div>
      </li>
      <li>
        <span class="stage-number" aria-hidden="true">03</span>
        <div>
          <h3>Movimiento</h3>
          <p>Una decisión observable: una conversación, una reducción, un bloque estratégico.</p>
        </div>
      </li>
    </ol>

    <a class="button performance-framework__cta" href="#zone-demo" data-track="zone_framework_cta_clicked" data-track-location="performance_framework">Construir mi siguiente etapa</a>
  </div>
</section>

<style>
  .performance-framework {
    --performance-accent: #e0702f;
    --performance-deep: #9c3f1b;
    --performance-soft: #f7e8de;
    background:
      linear-gradient(145deg, color-mix(in srgb, var(--surface) 78%, var(--performance-soft)), var(--surface-strong));
  }

  .performance-framework::before {
    position: absolute;
    top: -5rem;
    left: -8rem;
    width: 24rem;
    height: 15rem;
    border-radius: 38% 62% 48% 52% / 66% 40% 60% 34%;
    background: color-mix(in srgb, var(--performance-accent) 8%, transparent);
    content: '';
    transform: rotate(11deg);
  }

  .performance-framework .container {
    position: relative;
    z-index: 1;
  }

  .performance-framework__intro {
    max-width: 52rem;
  }

  .performance-framework .section-kicker {
    color: var(--performance-deep);
  }

  .performance-framework .section-title {
    max-width: 20ch;
    font-family: var(--font-performance);
    font-style: normal;
    font-weight: 600;
    letter-spacing: 0;
    line-height: 1.08;
  }

  .framework-stages {
    display: grid;
    gap: 1rem;
    margin: clamp(2.6rem, 7vw, 4.5rem) 0 0;
    padding: 0;
    list-style: none;
  }

  .framework-stages li {
    position: relative;
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: 1rem;
    min-height: 12rem;
    overflow: hidden;
    border: 1px solid color-mix(in srgb, var(--performance-deep) 16%, var(--line));
    border-radius: var(--radius-lg) var(--radius-lg) var(--radius-sm) var(--radius-lg);
    padding: clamp(1.35rem, 4vw, 2rem);
    background: color-mix(in srgb, var(--surface) 93%, var(--performance-soft));
    box-shadow: 0 18px 42px rgb(100 58 35 / 8%);
  }

  .framework-stages li::after {
    position: absolute;
    right: -3.5rem;
    bottom: -4.5rem;
    width: 10rem;
    height: 8rem;
    border-radius: 67% 33% 52% 48% / 39% 61% 39% 61%;
    background: color-mix(in srgb, var(--performance-accent) 9%, transparent);
    content: '';
    transform: rotate(-13deg);
  }

  .framework-stages li:nth-child(2)::after {
    background: color-mix(in srgb, var(--performance-deep) 8%, transparent);
    transform: rotate(8deg);
  }

  .stage-number {
    position: relative;
    z-index: 1;
    display: grid;
    width: 2.6rem;
    height: 2.6rem;
    place-items: center;
    border-radius: 45% 55% 59% 41% / 56% 43% 57% 44%;
    background: color-mix(in srgb, var(--performance-accent) 14%, var(--surface));
    color: var(--performance-deep);
    font-family: var(--font-body);
    font-size: 0.72rem;
    font-variant-numeric: tabular-nums;
    font-weight: 800;
  }

  .framework-stages li > div {
    position: relative;
    z-index: 1;
    padding-top: 0.2rem;
  }

  .framework-stages h3 {
    margin-bottom: 0.55rem;
    color: var(--text);
    font-family: var(--font-performance);
    font-size: clamp(1.35rem, 1.2rem + 0.65vw, 1.8rem);
    font-weight: 600;
  }

  .framework-stages p {
    max-width: 25rem;
    margin: 0;
    color: var(--text-muted);
    font-size: 0.98rem;
    line-height: 1.55;
  }

  .performance-framework__cta {
    margin-top: clamp(2rem, 5vw, 3rem);
    background: linear-gradient(
      120deg,
      color-mix(in srgb, var(--performance-accent) 48%, var(--performance-deep)),
      var(--performance-deep)
    );
    color: var(--white);
    box-shadow: 0 14px 30px rgb(156 63 27 / 24%);
  }

  .performance-framework__cta:hover {
    background: linear-gradient(
      120deg,
      color-mix(in srgb, var(--performance-accent) 52%, var(--performance-deep)),
      color-mix(in srgb, var(--performance-deep) 90%, var(--text))
    );
    box-shadow: 0 18px 36px rgb(156 63 27 / 29%);
  }

  .performance-framework__cta:focus-visible {
    outline-color: var(--surface);
    box-shadow: 0 0 0 5px var(--performance-deep);
  }

  @media (min-width: 48rem) {
    .framework-stages {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .framework-stages li:nth-child(2) {
      transform: translateY(1.25rem);
    }
  }
</style>
