<section
  class="section student-framework"
  id="framework-estudiante"
  aria-labelledby="student-framework-title"
  data-track-section="student_framework"
>
  <div class="container framework-layout">
    <header class="framework-heading">
      <span class="section-kicker">Sostén · Explora · Decide</span>
      <h2 class="section-title" id="student-framework-title">No tienes que elegir entre aguantar o dejarlo todo</h2>
      <a
        class="framework-action"
        href="#zone-demo"
        data-track="zone_framework_cta_clicked"
        data-track-location="student_framework"
      >
        Crear mi mapa de decisión
        <svg aria-hidden="true" viewBox="0 0 20 20" fill="none">
          <path d="M3 14c4-5 8-7 14-7M13 3l4 4-4 4" />
        </svg>
      </a>
    </header>

    <div class="framework-map">
      <svg class="framework-route" aria-hidden="true" viewBox="0 0 600 610" fill="none" preserveAspectRatio="none">
        <path d="M112 65c-44 87 42 136 124 149s172 58 123 142-183 58-116 190c30 58 107 40 220 0" />
        <path d="M354 356c66-23 115-12 156 38" />
      </svg>

      <ol class="framework-steps">
        <li class="framework-step">
          <span class="step-marker" aria-hidden="true">1</span>
          <div>
            <h3>Sostén</h3>
            <p>Protege el mínimo que te deja explorar</p>
          </div>
        </li>
        <li class="framework-step">
          <span class="step-marker" aria-hidden="true">2</span>
          <div>
            <h3>Explora</h3>
            <p>Prueba antes de decidir</p>
          </div>
        </li>
        <li class="framework-step">
          <span class="step-marker" aria-hidden="true">3</span>
          <div>
            <h3>Decide</h3>
            <p>Compara evidencias, no solo pensamientos</p>
          </div>
        </li>
      </ol>
    </div>
  </div>
</section>

<style>
  .student-framework {
    --student-gold: #d8b56d;
    --student-slate: #5d78a6;
    background:
      radial-gradient(ellipse at 12% 12%, color-mix(in srgb, var(--student-gold) 15%, transparent), transparent 34%),
      radial-gradient(ellipse at 88% 84%, color-mix(in srgb, var(--student-slate) 13%, transparent), transparent 40%),
      var(--surface);
  }

  .student-framework::before {
    position: absolute;
    bottom: -6rem;
    left: -7rem;
    width: 19rem;
    height: 15rem;
    border-radius: 59% 41% 31% 69% / 38% 57% 43% 62%;
    background: color-mix(in srgb, var(--student-gold) 9%, transparent);
    content: '';
    pointer-events: none;
    transform: rotate(11deg);
  }

  .framework-layout {
    display: grid;
    gap: clamp(3rem, 8vw, 6rem);
    align-items: center;
  }

  .framework-heading {
    position: relative;
    z-index: 1;
    max-width: 44rem;
  }

  .student-framework .section-kicker {
    color: color-mix(in srgb, var(--student-slate) 76%, var(--text));
  }

  .student-framework .section-title {
    max-width: 17ch;
    font-family: var(--font-student);
    font-style: normal;
    font-weight: 600;
    line-height: 1.08;
  }

  .framework-action {
    display: inline-flex;
    min-height: 3.35rem;
    align-items: center;
    justify-content: center;
    gap: 0.7rem;
    margin-top: 1rem;
    border: 1px solid color-mix(in srgb, var(--student-slate) 80%, var(--student-gold));
    border-radius: 999px;
    padding: 0.88rem 1.35rem;
    background:
      linear-gradient(
        120deg,
        color-mix(in srgb, var(--student-slate) 76%, var(--text)) 0%,
        color-mix(in srgb, var(--student-slate) 88%, var(--text)) 100%
      );
    color: var(--white);
    font-size: 0.95rem;
    font-weight: 760;
    line-height: 1.15;
    text-decoration: none;
    box-shadow: 0 14px 30px color-mix(in srgb, var(--student-slate) 24%, transparent);
    transition: transform 180ms ease, box-shadow 180ms ease, filter 180ms ease;
  }

  .framework-action:hover {
    box-shadow: 0 18px 36px color-mix(in srgb, var(--student-slate) 30%, transparent);
    filter: saturate(1.08);
    transform: translateY(-2px);
  }

  .framework-action svg {
    width: 1.1rem;
    flex: 0 0 auto;
  }

  .framework-action path {
    stroke: currentColor;
    stroke-width: 1.7;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .framework-map {
    position: relative;
    min-width: 0;
  }

  .framework-route {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    overflow: visible;
    pointer-events: none;
  }

  .framework-route path {
    stroke: color-mix(in srgb, var(--student-slate) 33%, transparent);
    stroke-width: 2;
    stroke-linecap: round;
    stroke-dasharray: 9 12;
  }

  .framework-route path:last-child {
    stroke: color-mix(in srgb, var(--student-gold) 52%, transparent);
  }

  .framework-steps {
    position: relative;
    display: grid;
    gap: 1rem;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .framework-step {
    position: relative;
    display: grid;
    grid-template-columns: 2.9rem 1fr;
    gap: 1rem;
    align-items: start;
    min-height: 9.5rem;
    border: 1px solid color-mix(in srgb, var(--student-slate) 18%, var(--line));
    border-radius: 1.7rem 1.7rem 1.7rem 0.75rem;
    padding: 1.35rem;
    background: color-mix(in srgb, var(--surface) 91%, var(--student-slate) 9%);
    box-shadow: var(--shadow-sm);
  }

  .framework-step:nth-child(2) {
    margin-left: clamp(0.75rem, 6vw, 3rem);
    border-radius: 1.7rem 0.75rem 1.7rem 1.7rem;
    background: color-mix(in srgb, var(--surface) 90%, var(--student-gold) 10%);
  }

  .framework-step:nth-child(3) {
    margin-right: clamp(0.75rem, 6vw, 3rem);
    border-radius: 0.75rem 1.7rem 1.7rem 1.7rem;
  }

  .step-marker {
    display: grid;
    width: 2.8rem;
    height: 2.6rem;
    place-items: center;
    border-radius: 64% 36% 56% 44% / 44% 62% 38% 56%;
    background: color-mix(in srgb, var(--student-gold) 24%, var(--surface));
    color: color-mix(in srgb, var(--student-slate) 88%, var(--text));
    font-family: var(--font-student);
    font-weight: 700;
    transform: rotate(-5deg);
  }

  .framework-step:nth-child(2) .step-marker {
    background: color-mix(in srgb, var(--student-slate) 16%, var(--surface));
    transform: rotate(5deg);
  }

  .framework-step h3 {
    margin-bottom: 0.4rem;
    color: color-mix(in srgb, var(--student-slate) 84%, var(--text));
    font-family: var(--font-student);
    font-size: clamp(1.35rem, 4vw, 1.75rem);
    font-weight: 600;
    line-height: 1.2;
  }

  .framework-step p {
    margin: 0;
    color: var(--text-muted);
    font-size: 0.93rem;
    line-height: 1.5;
  }

  @media (min-width: 54rem) {
    .framework-layout {
      grid-template-columns: minmax(0, 0.82fr) minmax(27rem, 1fr);
    }

    .framework-action {
      margin-top: 1.5rem;
    }
  }
</style>
