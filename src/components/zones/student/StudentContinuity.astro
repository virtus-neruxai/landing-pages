<section
  class="section student-continuity"
  id="continuidad-estudiante"
  aria-labelledby="student-continuity-title"
  data-track-section="student_continuity"
>
  <div class="container continuity-layout">
    <header class="continuity-heading">
      <span class="section-kicker">Continuidad</span>
      <h2 class="section-title" id="student-continuity-title">Tus dudas dejan de empezar de cero cada semana</h2>
      <p class="section-lede">Virtus conecta lo que cuentas, pruebas y haces para distinguir una mala semana de una dirección que ya no encaja.</p>
    </header>

    <div class="timeline-panel" aria-label="Ejemplo de continuidad durante siete días">
      <svg class="timeline-path" aria-hidden="true" viewBox="0 0 800 240" fill="none" preserveAspectRatio="none">
        <path d="M28 146c92-104 181 48 282-24s193-28 251 37 134 39 213-53" />
        <path d="M561 159c39-53 78-77 132-76" />
      </svg>

      <ol class="timeline">
        <li>
          <span class="timeline-day">Día 1</span>
          <strong>Lo que cuentas</strong>
        </li>
        <li>
          <span class="timeline-day">Día 4</span>
          <strong>Lo que pruebas</strong>
        </li>
        <li>
          <span class="timeline-day">Día 7</span>
          <strong>Lo que haces</strong>
        </li>
        <li class="timeline-result">
          <span class="timeline-day">Siguiente propuesta</span>
          <strong>Distinguir una mala semana de una dirección que ya no encaja</strong>
        </li>
      </ol>
    </div>

    <div class="continuity-footer">
      <p class="trust-line">
        <svg aria-hidden="true" viewBox="0 0 28 28" fill="none">
          <path d="M7.5 12.5V9a6.5 6.5 0 0 1 13 0v3.5" />
          <path d="M5.5 12.5h17v11h-17z" />
          <path d="M14 17v2.5" />
        </svg>
        <span>Tus reflexiones son privadas. Tú decides qué registrar y qué notificaciones recibir.</span>
      </p>

      <aside class="early-students" aria-label="Invitación a los primeros estudiantes">
        <svg aria-hidden="true" viewBox="0 0 54 54" fill="none">
          <path d="M10 42c10-4 17-13 21-29 10 7 15 16 12 24-4 10-17 13-33 5Z" />
          <path d="M12 42c11-8 19-18 24-30M24 29c6 0 11 2 16 6" />
        </svg>
        <p>Únete a los primeros estudiantes que usan Virtus para recuperar dirección.</p>
      </aside>
    </div>
  </div>
</section>

<style>
  .student-continuity {
    --student-gold: #d8b56d;
    --student-slate: #5d78a6;
    background:
      radial-gradient(ellipse at 90% 14%, color-mix(in srgb, var(--student-slate) 12%, transparent), transparent 36%),
      radial-gradient(ellipse at 8% 92%, color-mix(in srgb, var(--student-gold) 14%, transparent), transparent 35%),
      var(--background);
  }

  .continuity-layout {
    display: grid;
    gap: clamp(2.5rem, 7vw, 4.5rem);
  }

  .continuity-heading {
    max-width: 54rem;
  }

  .student-continuity .section-kicker {
    color: color-mix(in srgb, var(--student-slate) 76%, var(--text));
  }

  .student-continuity .section-title {
    max-width: 19ch;
    font-family: var(--font-student);
    font-style: normal;
    font-weight: 600;
    line-height: 1.08;
  }

  .student-continuity .section-lede {
    max-width: 48rem;
    margin-bottom: 0;
    font-size: clamp(1.05rem, 2.4vw, 1.3rem);
  }

  .timeline-panel {
    position: relative;
    overflow: hidden;
    border: 1px solid color-mix(in srgb, var(--student-slate) 20%, var(--line));
    border-radius: 2.3rem 2.3rem 0.9rem 2.3rem;
    padding: clamp(1.2rem, 5vw, 2.5rem);
    background:
      linear-gradient(145deg, color-mix(in srgb, var(--surface) 90%, var(--student-slate) 10%), color-mix(in srgb, var(--surface) 94%, var(--student-gold) 6%));
    box-shadow: var(--shadow-lg);
  }

  .timeline-panel::after {
    position: absolute;
    right: -5rem;
    bottom: -5.5rem;
    width: 15rem;
    height: 12rem;
    border-radius: 66% 34% 47% 53% / 43% 62% 38% 57%;
    background: color-mix(in srgb, var(--student-gold) 12%, transparent);
    content: '';
    pointer-events: none;
    transform: rotate(-9deg);
  }

  .timeline-path {
    display: none;
  }

  .timeline-path path {
    stroke: color-mix(in srgb, var(--student-slate) 42%, transparent);
    stroke-width: 2;
    stroke-linecap: round;
    stroke-dasharray: 8 10;
  }

  .timeline-path path:last-child {
    stroke: color-mix(in srgb, var(--student-gold) 62%, transparent);
  }

  .timeline {
    position: relative;
    z-index: 1;
    display: grid;
    gap: 0.8rem;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .timeline li {
    display: grid;
    gap: 0.35rem;
    min-height: 7rem;
    align-content: center;
    border: 1px solid color-mix(in srgb, var(--student-slate) 18%, var(--line));
    border-radius: 1.35rem 1.35rem 1.35rem 0.55rem;
    padding: 1rem;
    background: color-mix(in srgb, var(--surface) 88%, transparent);
  }

  .timeline li:nth-child(even) {
    border-radius: 1.35rem 0.55rem 1.35rem 1.35rem;
  }

  .timeline-day {
    color: color-mix(in srgb, var(--student-slate) 88%, var(--text));
    font-size: 0.72rem;
    font-weight: 780;
    line-height: 1.3;
  }

  .timeline strong {
    color: var(--text);
    font-family: var(--font-student);
    font-size: clamp(1rem, 3vw, 1.2rem);
    font-weight: 600;
    line-height: 1.35;
  }

  .timeline .timeline-result {
    border-color: color-mix(in srgb, var(--student-gold) 40%, var(--line));
    background: color-mix(in srgb, var(--surface) 82%, var(--student-gold) 18%);
  }

  .timeline-result .timeline-day {
    color: color-mix(in srgb, var(--student-slate) 78%, var(--text));
  }

  .continuity-footer {
    display: grid;
    gap: 1rem;
  }

  .trust-line,
  .early-students {
    margin: 0;
    border: 1px solid color-mix(in srgb, var(--student-slate) 17%, var(--line));
    background: color-mix(in srgb, var(--surface) 91%, var(--student-slate) 9%);
  }

  .trust-line {
    display: grid;
    grid-template-columns: 2rem 1fr;
    gap: 0.85rem;
    align-items: center;
    border-radius: 1.5rem 1.5rem 1.5rem 0.65rem;
    padding: 1.15rem;
    color: var(--text-muted);
    font-size: 0.88rem;
    line-height: 1.5;
  }

  .trust-line svg {
    width: 1.75rem;
    color: var(--student-slate);
  }

  .trust-line path {
    stroke: currentColor;
    stroke-width: 1.6;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .early-students {
    display: grid;
    grid-template-columns: 3.25rem 1fr;
    gap: 1rem;
    align-items: center;
    border-color: color-mix(in srgb, var(--student-gold) 30%, var(--line));
    border-radius: 1.5rem 0.65rem 1.5rem 1.5rem;
    padding: 1.15rem;
    background: color-mix(in srgb, var(--surface) 87%, var(--student-gold) 13%);
  }

  .early-students svg {
    width: 3rem;
    color: color-mix(in srgb, var(--student-slate) 82%, var(--text));
  }

  .early-students svg path:first-child {
    fill: color-mix(in srgb, var(--student-gold) 26%, transparent);
  }

  .early-students svg path:last-child {
    stroke: currentColor;
    stroke-width: 1.7;
    stroke-linecap: round;
  }

  .early-students p {
    margin: 0;
    color: color-mix(in srgb, var(--student-slate) 78%, var(--text));
    font-family: var(--font-student);
    font-size: clamp(1rem, 2.6vw, 1.18rem);
    font-weight: 600;
    line-height: 1.45;
  }

  @media (min-width: 44rem) {
    .timeline-panel {
      padding-block: 3rem;
    }

    .timeline-path {
      position: absolute;
      z-index: 0;
      inset: 9% 2%;
      display: block;
      width: 96%;
      height: 82%;
      overflow: visible;
    }

    .timeline {
      grid-template-columns: repeat(3, minmax(0, 0.78fr)) minmax(0, 1.3fr);
      align-items: center;
      gap: clamp(0.7rem, 1.8vw, 1.25rem);
    }

    .timeline li:nth-child(1) {
      transform: translateY(1.3rem);
    }

    .timeline li:nth-child(2) {
      transform: translateY(-1.1rem);
    }

    .timeline li:nth-child(3) {
      transform: translateY(0.7rem);
    }
  }

  @media (min-width: 52rem) {
    .continuity-footer {
      grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
    }
  }
</style>
