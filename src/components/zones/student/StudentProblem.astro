<section
  class="section student-problem"
  id="no-es-organizacion"
  aria-labelledby="student-problem-title"
  data-track-section="student_problem"
>
  <div class="container">
    <header class="problem-heading">
      <span class="section-kicker">No es organización</span>
      <h2 class="section-title" id="student-problem-title">Puede que no te falte disciplina. Puede que vayas por inercia.</h2>
    </header>

    <div class="problem-cards">
      <svg class="problem-route" aria-hidden="true" viewBox="0 0 1000 190" fill="none" preserveAspectRatio="none">
        <path d="M18 103C167 22 242 159 382 96S617 41 727 106s178 28 255-51" />
        <path d="M382 96c20-40 43-62 79-72" />
      </svg>

      <article class="problem-card problem-card--tired">
        <h3 class="card-label">Cansancio</h3>
        <blockquote>“Estoy agotado y todo me parece equivocado”</blockquote>
        <div class="card-direction">
          <svg aria-hidden="true" viewBox="0 0 32 24" fill="none">
            <path d="M2 17c8-8 16-10 27-8M24 4l5 5-5 5" />
          </svg>
          <p>Ajustar la carga antes de decidir</p>
        </div>
      </article>

      <article class="problem-card problem-card--fear">
        <h3 class="card-label">Miedo</h3>
        <blockquote>“Quiero cambiar pero me asusta equivocarme”</blockquote>
        <div class="card-direction">
          <svg aria-hidden="true" viewBox="0 0 32 24" fill="none">
            <path d="M2 17c8-8 16-10 27-8M24 4l5 5-5 5" />
          </svg>
          <p>Convertir la decisión en experimentos reversibles</p>
        </div>
      </article>

      <article class="problem-card problem-card--misalignment">
        <h3 class="card-label">Desalineación</h3>
        <blockquote>“Puedo seguir, pero ya no me reconozco”</blockquote>
        <div class="card-direction">
          <svg aria-hidden="true" viewBox="0 0 32 24" fill="none">
            <path d="M2 17c8-8 16-10 27-8M24 4l5 5-5 5" />
          </svg>
          <p>Explorar qué parte del camino ya no encaja</p>
        </div>
      </article>
    </div>

    <p class="problem-closing">No todo bloqueo significa abandonar. Pero no todo esfuerzo significa continuar.</p>
  </div>
</section>

<style>
  .student-problem {
    --student-gold: #d8b56d;
    --student-slate: #5d78a6;
    --student-gold-soft: color-mix(in srgb, var(--student-gold) 18%, var(--surface));
    --student-slate-soft: color-mix(in srgb, var(--student-slate) 12%, var(--surface));
    background:
      radial-gradient(ellipse at 92% 12%, color-mix(in srgb, var(--student-gold) 15%, transparent), transparent 35%),
      radial-gradient(ellipse at 4% 82%, color-mix(in srgb, var(--student-slate) 11%, transparent), transparent 37%),
      var(--background);
  }

  .student-problem::after {
    position: absolute;
    top: 8%;
    right: -7rem;
    width: 16rem;
    height: 11rem;
    border-radius: 38% 62% 71% 29% / 61% 37% 63% 39%;
    background: color-mix(in srgb, var(--student-gold) 8%, transparent);
    content: '';
    pointer-events: none;
    transform: rotate(-12deg);
  }

  .problem-heading {
    position: relative;
    z-index: 1;
    max-width: 54rem;
    margin-bottom: clamp(2.5rem, 7vw, 4.5rem);
  }

  .student-problem .section-kicker {
    color: color-mix(in srgb, var(--student-slate) 76%, var(--text));
  }

  .student-problem .section-title {
    max-width: 20ch;
    margin-bottom: 0;
    font-family: var(--font-student);
    font-style: normal;
    font-weight: 600;
    line-height: 1.08;
  }

  .problem-cards {
    position: relative;
    display: grid;
    gap: 1rem;
  }

  .problem-route {
    display: none;
  }

  .problem-route path {
    stroke: color-mix(in srgb, var(--student-slate) 44%, transparent);
    stroke-width: 2;
    stroke-linecap: round;
    stroke-dasharray: 8 11;
  }

  .problem-route path:last-child {
    stroke: color-mix(in srgb, var(--student-gold) 58%, transparent);
  }

  .problem-card {
    position: relative;
    z-index: 1;
    display: flex;
    min-height: 19rem;
    flex-direction: column;
    overflow: hidden;
    border: 1px solid color-mix(in srgb, var(--student-slate) 18%, var(--line));
    border-radius: 2rem 2rem 2rem 0.85rem;
    padding: clamp(1.3rem, 4vw, 2rem);
    background:
      linear-gradient(145deg, var(--student-slate-soft), color-mix(in srgb, var(--surface) 96%, transparent) 66%);
    box-shadow: var(--shadow-sm);
  }

  .problem-card::after {
    position: absolute;
    right: -4rem;
    bottom: -4.5rem;
    width: 10rem;
    height: 9rem;
    border-radius: 64% 36% 42% 58% / 44% 61% 39% 56%;
    background: color-mix(in srgb, var(--student-gold) 13%, transparent);
    content: '';
    pointer-events: none;
    transform: rotate(16deg);
  }

  .problem-card--fear {
    border-radius: 2rem 0.85rem 2rem 2rem;
    background:
      linear-gradient(155deg, var(--student-gold-soft), color-mix(in srgb, var(--surface) 96%, transparent) 70%);
  }

  .problem-card--misalignment {
    border-radius: 0.85rem 2rem 2rem 2rem;
  }

  .card-label {
    position: relative;
    z-index: 1;
    width: fit-content;
    margin-bottom: 1.5rem;
    border: 1px solid color-mix(in srgb, var(--student-gold) 48%, transparent);
    border-radius: 999px;
    padding: 0.4rem 0.75rem;
    background: color-mix(in srgb, var(--student-gold) 12%, var(--surface));
    color: color-mix(in srgb, var(--student-slate) 88%, var(--text));
    font-size: 0.75rem;
    font-weight: 750;
  }

  blockquote {
    position: relative;
    z-index: 1;
    margin: 0 0 2rem;
    color: var(--text);
    font-family: var(--font-student);
    font-size: clamp(1.25rem, 4vw, 1.65rem);
    font-weight: 600;
    line-height: 1.3;
  }

  .card-direction {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 2rem 1fr;
    gap: 0.65rem;
    align-items: start;
    margin-top: auto;
    border-top: 1px solid color-mix(in srgb, var(--student-slate) 20%, var(--line));
    padding-top: 1.2rem;
  }

  .card-direction svg {
    width: 1.8rem;
    margin-top: 0.2rem;
    color: var(--student-slate);
  }

  .card-direction path {
    stroke: currentColor;
    stroke-width: 1.7;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .card-direction p {
    margin: 0;
    color: var(--text-muted);
    font-size: 0.9rem;
    line-height: 1.5;
  }

  .problem-closing {
    position: relative;
    z-index: 1;
    max-width: 48rem;
    margin: clamp(2rem, 6vw, 4rem) auto 0;
    border-left: 3px solid var(--student-gold);
    padding: 0.25rem 0 0.25rem clamp(1rem, 3vw, 1.5rem);
    color: color-mix(in srgb, var(--student-slate) 78%, var(--text));
    font-family: var(--font-student);
    font-size: clamp(1.15rem, 3vw, 1.5rem);
    font-weight: 600;
    line-height: 1.45;
  }

  @media (min-width: 52rem) {
    .problem-cards {
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: clamp(1rem, 2.4vw, 2rem);
      padding-block: 1.5rem;
    }

    .problem-route {
      position: absolute;
      z-index: 0;
      top: -3.5rem;
      left: 2%;
      display: block;
      width: 96%;
      height: 7rem;
      overflow: visible;
    }

    .problem-card--tired {
      transform: translateY(1.25rem);
    }

    .problem-card--misalignment {
      transform: translateY(-1.25rem);
    }
  }
</style>
