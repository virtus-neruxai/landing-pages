<section
  class="section spiritual-framework"
  id="raiz-valores-proposito"
  aria-labelledby="spiritual-framework-title"
  data-track-section="spiritual_framework"
>
  <div class="container spiritual-framework__layout">
    <header class="spiritual-framework__heading">
      <span class="section-kicker">Raíz · Valores · Propósito</span>
      <h2 class="section-title" id="spiritual-framework-title">El propósito no se encuentra de una vez. Se cultiva.</h2>
      <p class="section-lede">
        Virtus te ayuda a pasar de una intuición profunda a una forma concreta de elegir, actuar y revisar lo vivido.
      </p>
      <a
        class="spiritual-framework__action"
        href="#zone-demo"
        data-track="zone_framework_cta_clicked"
        data-track-location="spiritual_framework"
      >
        Convertir mis valores en práctica
        <svg aria-hidden="true" viewBox="0 0 20 20" fill="none">
          <path d="M3 14c4-5 8-7 14-7M13 3l4 4-4 4" />
        </svg>
      </a>
    </header>

    <div class="growth-map">
      <svg class="growth-map__stem" aria-hidden="true" viewBox="0 0 560 610" fill="none" preserveAspectRatio="none">
        <path d="M114 550c82-73 49-132 132-188s62-128 145-186c48-34 61-79 55-129" />
        <path d="M249 360c-53-11-91-1-126 32M391 176c-41-22-80-25-119-7M391 176c46-1 80 12 108 39" />
      </svg>

      <ol class="growth-steps">
        <li class="growth-step growth-step--root">
          <span class="growth-marker" aria-hidden="true">01</span>
          <div>
            <h3>Raíz</h3>
            <p>Reconoce qué te sostiene cuando alrededor todo cambia.</p>
          </div>
        </li>
        <li class="growth-step growth-step--values">
          <span class="growth-marker" aria-hidden="true">02</span>
          <div>
            <h3>Valores</h3>
            <p>Usa lo importante como criterio para elegir y renunciar.</p>
          </div>
        </li>
        <li class="growth-step growth-step--purpose">
          <span class="growth-marker" aria-hidden="true">03</span>
          <div>
            <h3>Propósito</h3>
            <p>Lleva ese criterio a una práctica pequeña y observable.</p>
          </div>
        </li>
      </ol>
    </div>
  </div>
</section>

<style>
  .spiritual-framework {
    --spiritual-forest: #2f6b4f;
    --spiritual-deep: #1f4e39;
    --spiritual-sage: #8aa66a;
    background:
      radial-gradient(ellipse at 13% 11%, color-mix(in srgb, var(--spiritual-sage) 15%, transparent), transparent 34rem),
      radial-gradient(ellipse at 90% 88%, color-mix(in srgb, var(--spiritual-forest) 10%, transparent), transparent 37rem),
      var(--surface);
  }

  .spiritual-framework::before {
    position: absolute;
    bottom: -6.5rem;
    left: -7.5rem;
    width: 20rem;
    height: 16rem;
    border-radius: 62% 38% 31% 69% / 38% 59% 41% 62%;
    background: color-mix(in srgb, var(--spiritual-sage) 9%, transparent);
    content: '';
    pointer-events: none;
    transform: rotate(10deg);
  }

  .spiritual-framework__layout {
    display: grid;
    gap: clamp(3rem, 8vw, 6rem);
    align-items: center;
  }

  .spiritual-framework__heading {
    position: relative;
    z-index: 1;
    max-width: 46rem;
  }

  .spiritual-framework .section-kicker {
    color: var(--spiritual-deep);
  }

  .spiritual-framework .section-title {
    max-width: 18ch;
    font-family: var(--font-spiritual);
    font-style: italic;
    font-weight: 500;
    letter-spacing: 0;
    line-height: 1.07;
  }

  .spiritual-framework .section-lede {
    margin-bottom: 0;
  }

  .spiritual-framework__action {
    display: inline-flex;
    min-height: 3.35rem;
    align-items: center;
    justify-content: center;
    gap: 0.7rem;
    margin-top: 1.5rem;
    border: 1px solid transparent;
    border-radius: 999px;
    padding: 0.88rem 1.35rem;
    background: linear-gradient(120deg, var(--spiritual-forest), var(--spiritual-deep));
    color: var(--white);
    font-family: var(--font-body);
    font-size: 0.95rem;
    font-weight: 760;
    line-height: 1.2;
    text-decoration: none;
    box-shadow: 0 14px 30px rgb(47 107 79 / 23%);
    transition: transform 180ms ease, box-shadow 180ms ease, filter 180ms ease;
  }

  .spiritual-framework__action:hover {
    box-shadow: 0 18px 36px rgb(47 107 79 / 29%);
    filter: saturate(1.08);
    transform: translateY(-2px);
  }

  .spiritual-framework__action:focus-visible {
    outline-color: var(--surface);
    box-shadow: 0 0 0 5px var(--spiritual-deep);
  }

  .spiritual-framework__action svg {
    width: 1.1rem;
    flex: 0 0 auto;
  }

  .spiritual-framework__action path {
    stroke: currentColor;
    stroke-width: 1.7;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .growth-map {
    position: relative;
    min-width: 0;
  }

  .growth-map__stem {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    overflow: visible;
    pointer-events: none;
  }

  .growth-map__stem path {
    stroke: color-mix(in srgb, var(--spiritual-forest) 35%, transparent);
    stroke-width: 2;
    stroke-linecap: round;
  }

  .growth-map__stem path:nth-child(2) {
    stroke: color-mix(in srgb, var(--spiritual-sage) 57%, transparent);
  }

  .growth-steps {
    position: relative;
    display: grid;
    gap: 1rem;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .growth-step {
    position: relative;
    display: grid;
    grid-template-columns: 3rem minmax(0, 1fr);
    gap: 1rem;
    align-items: start;
    min-height: 9.6rem;
    overflow: hidden;
    border: 1px solid color-mix(in srgb, var(--spiritual-forest) 18%, var(--line));
    border-radius: 1.8rem 1.8rem 1.8rem 0.7rem;
    padding: 1.35rem;
    background: color-mix(in srgb, var(--surface) 90%, var(--spiritual-sage) 10%);
    box-shadow: 0 13px 32px rgb(31 78 57 / 8%);
  }

  .growth-step::after {
    position: absolute;
    right: -3.5rem;
    bottom: -4.5rem;
    width: 9rem;
    height: 8rem;
    border-radius: 67% 33% 52% 48% / 39% 61% 39% 61%;
    background: color-mix(in srgb, var(--spiritual-sage) 10%, transparent);
    content: '';
  }

  .growth-step--values {
    margin-left: clamp(0.75rem, 6vw, 3rem);
    border-radius: 1.8rem 0.7rem 1.8rem 1.8rem;
    background: color-mix(in srgb, var(--surface) 91%, var(--spiritual-forest) 9%);
  }

  .growth-step--purpose {
    margin-right: clamp(0.75rem, 6vw, 3rem);
    border-radius: 0.7rem 1.8rem 1.8rem 1.8rem;
  }

  .growth-marker {
    position: relative;
    z-index: 1;
    display: grid;
    width: 2.8rem;
    height: 2.7rem;
    place-items: center;
    border-radius: 65% 35% 58% 42% / 45% 63% 37% 55%;
    background: color-mix(in srgb, var(--spiritual-sage) 24%, var(--surface));
    color: var(--spiritual-deep);
    font-family: var(--font-body);
    font-size: 0.72rem;
    font-variant-numeric: tabular-nums;
    font-weight: 800;
    transform: rotate(-5deg);
  }

  .growth-step--values .growth-marker {
    background: color-mix(in srgb, var(--spiritual-forest) 15%, var(--surface));
    transform: rotate(5deg);
  }

  .growth-step > div {
    position: relative;
    z-index: 1;
  }

  .growth-step h3 {
    margin: 0 0 0.4rem;
    color: var(--spiritual-deep);
    font-family: var(--font-spiritual);
    font-size: clamp(1.4rem, 4vw, 1.8rem);
    font-style: italic;
    font-weight: 500;
    line-height: 1.2;
  }

  .growth-step p {
    max-width: 26rem;
    margin: 0;
    color: var(--text-muted);
    font-size: 0.93rem;
    line-height: 1.52;
  }

  @media (min-width: 54rem) {
    .spiritual-framework__layout {
      grid-template-columns: minmax(0, 0.84fr) minmax(27rem, 1fr);
    }
  }
</style>
