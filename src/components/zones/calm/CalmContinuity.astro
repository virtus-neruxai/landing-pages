<section
  class="section calm-continuity"
  id="ritmo-sostenible"
  aria-labelledby="calm-continuity-title"
  data-track-section="calm_continuity"
>
  <div class="container calm-continuity__layout">
    <header class="calm-continuity__heading">
      <span class="section-kicker">Un ritmo que aprende contigo</span>
      <h2 class="section-title" id="calm-continuity-title">
        Tu plan se ajusta antes de convertirse en otra carga
      </h2>
      <p class="section-lede">
        Virtus relaciona cómo llegas, lo que intentas y lo que puedes sostener. Así una semana difícil cambia el siguiente paso, no el valor que te das.
      </p>
    </header>

    <figure class="rhythm-panel" aria-labelledby="calm-rhythm-caption">
      <figcaption class="sr-only" id="calm-rhythm-caption">
        Ejemplo de cómo Virtus transforma el contexto de una semana exigente en un siguiente paso sostenible
      </figcaption>

      <svg class="rhythm-path" aria-hidden="true" viewBox="0 0 920 250" fill="none" preserveAspectRatio="none">
        <path d="M24 144c105-82 173 49 281-18s194-33 277 28 173 59 313-44" />
      </svg>

      <ol class="rhythm-steps">
        <li>
          <span>Cómo llegas</span>
          <strong>Sueño corto y energía baja</strong>
        </li>
        <li>
          <span>Qué pesa</span>
          <strong>Tres pendientes compiten por atención</strong>
        </li>
        <li>
          <span>Qué sí cabe</span>
          <strong>Un bloque breve y una pausa real</strong>
        </li>
        <li class="rhythm-result">
          <span>Siguiente propuesta</span>
          <strong>Protege lo esencial y deja el resto para cuando vuelva el espacio</strong>
        </li>
      </ol>
    </figure>

    <div class="calm-continuity__footer">
      <p class="trust-line">
        <svg aria-hidden="true" viewBox="0 0 28 28" fill="none">
          <path d="M7.5 12.5V9a6.5 6.5 0 0 1 13 0v3.5" />
          <path d="M5.5 12.5h17v11h-17z" />
          <path d="M14 17v2.5" />
        </svg>
        <span>Tú decides qué registrar. Virtus observa hábitos y contexto; no hace diagnósticos.</span>
      </p>

      <aside class="calm-audience" aria-label="A quién acompaña el perfil Calma">
        <svg aria-hidden="true" viewBox="0 0 58 42" fill="none">
          <path d="M3 15c8-8 16-8 24 0s16 8 28 0" />
          <path d="M3 27c8-8 16-8 24 0s16 8 28 0" />
        </svg>
        <p>Para quienes necesitan recuperar capacidad sin convertir el descanso en otra meta.</p>
      </aside>
    </div>
  </div>
</section>

<style>
  .calm-continuity {
    --calm-blue: #2f80ed;
    --calm-deep: #174a7c;
    --calm-soft: #e6f1ff;
    background:
      radial-gradient(ellipse at 92% 12%, color-mix(in srgb, var(--calm-blue) 11%, transparent), transparent 35rem),
      radial-gradient(ellipse at 7% 91%, color-mix(in srgb, var(--calm-deep) 7%, transparent), transparent 32rem),
      var(--background);
  }

  .calm-continuity__layout {
    display: grid;
    gap: clamp(2.5rem, 7vw, 4.5rem);
  }

  .calm-continuity__heading {
    max-width: 55rem;
  }

  .calm-continuity .section-kicker {
    color: var(--calm-deep);
  }

  .calm-continuity .section-title {
    max-width: 20ch;
    font-family: var(--font-calm);
    font-style: normal;
    font-weight: 700;
    line-height: 1.08;
  }

  .calm-continuity .section-lede {
    max-width: 48rem;
    margin-bottom: 0;
  }

  .rhythm-panel {
    position: relative;
    overflow: hidden;
    margin: 0;
    border: 1px solid color-mix(in srgb, var(--calm-deep) 18%, var(--line));
    border-radius: 2.4rem 2.4rem 0.95rem 2.4rem;
    padding: clamp(1.2rem, 5vw, 2.5rem);
    background: linear-gradient(145deg, color-mix(in srgb, var(--surface) 86%, var(--calm-soft)), var(--surface));
    box-shadow: 0 26px 64px rgb(23 74 124 / 12%);
  }

  .rhythm-panel::after {
    position: absolute;
    right: -5rem;
    bottom: -5.5rem;
    width: 16rem;
    height: 12rem;
    border-radius: 66% 34% 47% 53% / 43% 62% 38% 57%;
    background: color-mix(in srgb, var(--calm-blue) 10%, transparent);
    content: '';
    pointer-events: none;
    transform: rotate(-8deg);
  }

  .rhythm-path {
    display: none;
  }

  .rhythm-path path {
    stroke: color-mix(in srgb, var(--calm-blue) 38%, transparent);
    stroke-width: 2.2;
    stroke-linecap: round;
    stroke-dasharray: 8 10;
  }

  .rhythm-steps {
    position: relative;
    z-index: 1;
    display: grid;
    gap: 0.8rem;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .rhythm-steps li {
    display: grid;
    gap: 0.35rem;
    min-height: 7rem;
    align-content: center;
    border: 1px solid color-mix(in srgb, var(--calm-deep) 14%, var(--line));
    border-radius: 1.5rem 1.5rem 1.5rem 0.6rem;
    padding: 1rem;
    background: color-mix(in srgb, var(--surface) 90%, transparent);
  }

  .rhythm-steps li:nth-child(even) {
    border-radius: 1.5rem 0.6rem 1.5rem 1.5rem;
  }

  .rhythm-steps span {
    color: var(--calm-deep);
    font-size: 0.72rem;
    font-weight: 800;
    line-height: 1.3;
  }

  .rhythm-steps strong {
    color: var(--text);
    font-family: var(--font-calm);
    font-size: clamp(1rem, 0.94rem + 0.3vw, 1.18rem);
    font-weight: 700;
    line-height: 1.38;
  }

  .rhythm-steps .rhythm-result {
    border-color: color-mix(in srgb, var(--calm-blue) 32%, var(--line));
    background: color-mix(in srgb, var(--surface) 77%, var(--calm-soft));
  }

  .calm-continuity__footer {
    display: grid;
    gap: 1rem;
  }

  .trust-line,
  .calm-audience {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: 0.85rem;
    align-items: center;
    margin: 0;
    border: 1px solid color-mix(in srgb, var(--calm-deep) 15%, var(--line));
    border-radius: var(--radius-md);
    padding: 1rem 1.1rem;
    background: color-mix(in srgb, var(--surface) 91%, var(--calm-soft));
    color: var(--text-muted);
    font-size: 0.86rem;
    line-height: 1.52;
  }

  .trust-line svg {
    width: 1.7rem;
    color: var(--calm-deep);
  }

  .trust-line path,
  .calm-audience path {
    stroke: currentColor;
    stroke-width: 1.65;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .calm-audience {
    color: var(--calm-deep);
    font-family: var(--font-calm);
    font-weight: 700;
  }

  .calm-audience svg {
    width: 2.4rem;
    color: var(--calm-blue);
  }

  .calm-audience p,
  .trust-line span {
    margin: 0;
  }

  @media (min-width: 52rem) {
    .rhythm-path {
      position: absolute;
      z-index: 0;
      top: 1.8rem;
      left: 2%;
      display: block;
      width: 96%;
      height: calc(100% - 3.6rem);
      pointer-events: none;
    }

    .rhythm-steps {
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 0.9rem;
      align-items: center;
    }

    .rhythm-steps li:nth-child(2),
    .rhythm-steps li:nth-child(4) {
      transform: translateY(0.8rem);
    }

    .calm-continuity__footer {
      grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
    }
  }
</style>
