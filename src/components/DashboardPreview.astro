<section class="section dashboard-section" id="dashboard" aria-labelledby="dashboard-title" data-track-section="dashboard">
  <div class="container">
    <div class="dashboard-heading">
      <div>
        <span class="section-kicker">Tu progreso, sin ruido</span>
        <h2 class="section-title" id="dashboard-title">Todo tu progreso, en un solo sitio</h2>
      </div>
      <p class="section-lede">Métricas reales de tu avance, y desafíos que se convierten en rutina para seguir creciendo.</p>
    </div>

    <div class="dashboard-stage">
      <div class="dashboard-mockup" role="img" aria-label="Vista ilustrativa del dashboard de Virtus">
        <div class="mock-sidebar" aria-hidden="true">
          <div class="mock-brand">
            <span></span>
            <strong>Virtus</strong>
          </div>
          <div class="mock-nav">
            <i class="active"></i><i></i><i></i><i></i><i></i>
          </div>
          <div class="mock-avatar"></div>
        </div>

        <div class="mock-main">
          <div class="mock-header">
            <div>
              <span class="mock-eyebrow">MARTES · 21 JULIO</span>
              <h3>Tu brújula de hoy</h3>
            </div>
            <div class="level-chip"><span>NIVEL</span><strong>12</strong></div>
          </div>

          <div class="mock-metrics">
            <div>
              <span>PROGRESO SEMANAL</span>
              <strong>68%</strong>
              <div class="metric-bar"><i></i></div>
            </div>
            <div>
              <span>ENERGÍA MEDIA</span>
              <strong>3.6 <small>/ 5</small></strong>
              <div class="metric-spark" aria-hidden="true">
                <i></i><i></i><i></i><i></i><i></i><i></i><i></i>
              </div>
            </div>
          </div>

          <div class="mock-content-grid">
            <div class="task-panel">
              <div class="panel-heading">
                <div><span class="panel-dot"></span><strong>Siguientes pasos</strong></div>
                <span>3 HOY</span>
              </div>
              <ul>
                <li class="complete"><span class="mock-check">✓</span><div><strong>Definir el entregable mínimo</strong><small>Proyecto final · 15 min</small></div><time>09:20</time></li>
                <li><span class="mock-check"></span><div><strong>Primer bloque sin distracciones</strong><small>Rutina de foco · 25 min</small></div><time>17:00</time></li>
                <li><span class="mock-check"></span><div><strong>Registrar cómo llegas</strong><small>Check-in · 2 min</small></div><time>21:30</time></li>
              </ul>
            </div>

            <div class="mission-panel">
              <div class="mission-icon" aria-hidden="true">↗</div>
              <span>MISIÓN ACTIVA</span>
              <h4>Terminar la carrera sin perderme por el camino</h4>
              <div class="mission-progress"><i></i></div>
              <div class="mission-meta"><span>Semana 3 de 8</span><strong>42%</strong></div>
            </div>
          </div>

          <div class="challenge-strip">
            <span class="challenge-label">DESAFÍO EN CURSO</span>
            <strong>7 días empezando por la tarea más pequeña</strong>
            <span class="challenge-days"><i class="done"></i><i class="done"></i><i class="done"></i><i class="today"></i><i></i><i></i><i></i></span>
          </div>
        </div>
      </div>

      <div class="annotation annotation--tasks"><span>01</span> Tus siguientes pasos, sin una lista infinita</div>
      <div class="annotation annotation--mission"><span>02</span> Una misión que da sentido a cada tarea</div>
      <div class="annotation annotation--context"><span>03</span> Avance medido con tu contexto real</div>
      <p class="dashboard-caption">Vista ilustrativa del producto · contenido de ejemplo</p>
    </div>
  </div>
</section>

<style>
  .dashboard-section {
    background: var(--paper);
  }

  .dashboard-heading {
    display: grid;
    gap: 1rem 4rem;
    align-items: end;
    margin-bottom: 4.5rem;
  }

  .dashboard-heading .section-title,
  .dashboard-heading .section-lede {
    margin-bottom: 0;
  }

  .dashboard-stage {
    position: relative;
    padding-bottom: 8rem;
  }

  .dashboard-mockup {
    display: grid;
    min-height: 34rem;
    overflow: hidden;
    border: 1px solid rgb(23 24 28 / 18%);
    border-radius: var(--radius-lg);
    background: #ece8df;
    box-shadow: 0 35px 90px rgb(23 24 28 / 18%);
  }

  .mock-sidebar {
    display: none;
    flex-direction: column;
    align-items: center;
    justify-content: space-between;
    padding: 1.2rem 0.75rem;
    background: var(--ink);
    color: var(--bone);
  }

  .mock-brand {
    display: grid;
    justify-items: center;
    gap: 0.35rem;
  }

  .mock-brand span {
    width: 1.45rem;
    height: 1.45rem;
    border: 1px solid var(--ochre);
    border-radius: 50%;
    background: radial-gradient(circle, var(--ochre) 0 16%, transparent 18%);
  }

  .mock-brand strong {
    font-size: 0.54rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .mock-nav {
    display: grid;
    gap: 1.15rem;
  }

  .mock-nav i {
    width: 1.25rem;
    height: 1.25rem;
    border: 1px solid rgb(242 238 230 / 26%);
    border-radius: 0.3rem;
  }

  .mock-nav i.active {
    border-color: var(--ochre);
    background: rgb(199 154 62 / 22%);
  }

  .mock-avatar {
    width: 1.8rem;
    height: 1.8rem;
    border-radius: 50%;
    background: var(--indigo);
  }

  .mock-main {
    min-width: 0;
    padding: clamp(1rem, 3vw, 2.2rem);
  }

  .mock-header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .mock-eyebrow,
  .mock-metrics span,
  .panel-heading,
  .mission-panel > span,
  .challenge-label {
    color: var(--muted);
    font-family: var(--font-mono);
    font-size: 0.53rem;
    letter-spacing: 0.09em;
  }

  .mock-header h3 {
    margin: 0.2rem 0 0;
    font-family: var(--font-display);
    font-size: clamp(1.55rem, 4vw, 2.2rem);
    font-weight: 590;
    letter-spacing: -0.035em;
  }

  .level-chip {
    display: grid;
    grid-template-columns: auto auto;
    gap: 0.5rem;
    align-items: center;
    border: 1px solid rgb(199 154 62 / 32%);
    border-radius: 999px;
    padding: 0.35rem 0.65rem;
    background: var(--ochre-soft);
    color: #6b501a;
    font-family: var(--font-mono);
  }

  .level-chip span { font-size: 0.5rem; letter-spacing: 0.08em; }
  .level-chip strong { font-size: 0.73rem; }

  .mock-metrics {
    display: grid;
    gap: 0.7rem;
    margin-bottom: 0.7rem;
  }

  .mock-metrics > div {
    min-height: 6.8rem;
    border: 1px solid var(--line);
    border-radius: var(--radius-sm);
    padding: 1rem;
    background: var(--white);
  }

  .mock-metrics strong {
    display: block;
    margin-top: 0.25rem;
    font-family: var(--font-mono);
    font-size: 1.35rem;
  }

  .mock-metrics strong small { color: var(--muted); font-size: 0.6rem; }

  .metric-bar {
    height: 0.35rem;
    margin-top: 0.65rem;
    overflow: hidden;
    border-radius: 1rem;
    background: var(--indigo-soft);
  }

  .metric-bar i {
    display: block;
    width: 68%;
    height: 100%;
    background: var(--indigo);
  }

  .metric-spark {
    display: flex;
    height: 1.6rem;
    align-items: end;
    gap: 0.24rem;
    margin-top: 0.4rem;
  }

  .metric-spark i {
    width: 0.35rem;
    border-radius: 0.15rem 0.15rem 0 0;
    background: var(--ochre);
  }

  .metric-spark i:nth-child(1) { height: 35%; }
  .metric-spark i:nth-child(2) { height: 55%; }
  .metric-spark i:nth-child(3) { height: 44%; }
  .metric-spark i:nth-child(4) { height: 72%; }
  .metric-spark i:nth-child(5) { height: 63%; }
  .metric-spark i:nth-child(6) { height: 78%; }
  .metric-spark i:nth-child(7) { height: 58%; }

  .mock-content-grid {
    display: grid;
    gap: 0.7rem;
  }

  .task-panel,
  .mission-panel,
  .challenge-strip {
    border: 1px solid var(--line);
    border-radius: var(--radius-sm);
    background: var(--white);
  }

  .task-panel {
    padding: 1rem;
  }

  .panel-heading {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-bottom: 0.65rem;
  }

  .panel-heading > div {
    display: flex;
    align-items: center;
    gap: 0.4rem;
  }

  .panel-heading strong {
    color: var(--ink);
    font-family: var(--font-body);
    font-size: 0.7rem;
    letter-spacing: 0;
  }

  .panel-dot {
    width: 0.4rem;
    height: 0.4rem;
    border-radius: 50%;
    background: var(--indigo);
  }

  .task-panel ul {
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .task-panel li {
    display: grid;
    grid-template-columns: auto 1fr auto;
    gap: 0.55rem;
    align-items: center;
    border-top: 1px solid var(--line);
    padding: 0.62rem 0;
  }

  .mock-check {
    display: grid;
    width: 1rem;
    height: 1rem;
    place-items: center;
    border: 1px solid rgb(23 24 28 / 24%);
    border-radius: 50%;
    font-size: 0.55rem;
  }

  li.complete .mock-check {
    border-color: var(--indigo);
    background: var(--indigo);
    color: var(--white);
  }

  .task-panel li strong,
  .task-panel li small {
    display: block;
  }

  .task-panel li strong { font-size: 0.62rem; }
  .task-panel li small { color: var(--muted); font-size: 0.52rem; }
  .task-panel time { color: var(--muted); font-family: var(--font-mono); font-size: 0.48rem; }

  .mission-panel {
    position: relative;
    overflow: hidden;
    padding: 1rem;
    background: var(--indigo-deep);
    color: var(--bone);
  }

  .mission-panel::after {
    position: absolute;
    right: -3rem;
    bottom: -3rem;
    width: 8rem;
    height: 8rem;
    border: 1px solid rgb(242 238 230 / 13%);
    border-radius: 50%;
    content: '';
  }

  .mission-icon {
    display: grid;
    width: 2rem;
    height: 2rem;
    place-items: center;
    margin-bottom: 1rem;
    border: 1px solid rgb(242 238 230 / 20%);
    border-radius: 50%;
    color: var(--ochre);
  }

  .mission-panel > span { color: rgb(242 238 230 / 55%); }

  .mission-panel h4 {
    max-width: 17rem;
    margin: 0.4rem 0 1.3rem;
    font-family: var(--font-display);
    font-size: 1.15rem;
    font-weight: 560;
    line-height: 1.2;
  }

  .mission-progress {
    height: 0.28rem;
    overflow: hidden;
    border-radius: 1rem;
    background: rgb(242 238 230 / 13%);
  }

  .mission-progress i { display: block; width: 42%; height: 100%; background: var(--ochre); }

  .mission-meta {
    display: flex;
    justify-content: space-between;
    margin-top: 0.45rem;
    color: rgb(242 238 230 / 56%);
    font-family: var(--font-mono);
    font-size: 0.48rem;
  }

  .mission-meta strong { color: var(--ochre); }

  .challenge-strip {
    display: none;
    grid-template-columns: auto 1fr auto;
    gap: 1rem;
    align-items: center;
    margin-top: 0.7rem;
    padding: 0.85rem 1rem;
  }

  .challenge-strip > strong { font-size: 0.65rem; }

  .challenge-days {
    display: flex;
    gap: 0.27rem;
  }

  .challenge-days i {
    width: 0.48rem;
    height: 0.48rem;
    border: 1px solid var(--line);
    border-radius: 50%;
  }

  .challenge-days i.done { border-color: var(--indigo); background: var(--indigo); }
  .challenge-days i.today { border: 2px solid var(--ochre); }

  .annotation {
    position: absolute;
    display: none;
    max-width: 14rem;
    align-items: center;
    gap: 0.6rem;
    color: var(--muted);
    font-family: var(--font-mono);
    font-size: 0.6rem;
    line-height: 1.45;
  }

  .annotation span {
    display: grid;
    width: 1.65rem;
    height: 1.65rem;
    flex: 0 0 auto;
    place-items: center;
    border-radius: 50%;
    background: var(--indigo);
    color: var(--bone);
    font-size: 0.54rem;
  }

  .dashboard-caption {
    position: absolute;
    right: 0;
    bottom: 5.2rem;
    margin: 0;
    color: var(--muted);
    font-size: 0.6rem;
  }

  @media (min-width: 35rem) {
    .mock-metrics {
      grid-template-columns: 1fr 1fr;
    }

    .challenge-strip {
      display: grid;
    }
  }

  @media (min-width: 54rem) {
    .dashboard-heading {
      grid-template-columns: minmax(0, 1fr) minmax(20rem, 0.7fr);
    }

    .dashboard-mockup {
      grid-template-columns: 5rem minmax(0, 1fr);
    }

    .mock-sidebar {
      display: flex;
    }

    .mock-content-grid {
      grid-template-columns: minmax(0, 1.35fr) minmax(14rem, 0.65fr);
    }
  }

  @media (min-width: 70rem) {
    .dashboard-stage {
      padding-inline: 3rem;
    }

    .annotation {
      display: flex;
    }

    .annotation--tasks { bottom: 3rem; left: 6rem; }
    .annotation--mission { right: 5rem; bottom: 3rem; }
    .annotation--context { top: -2.8rem; right: 4rem; }
    .dashboard-caption { right: 3rem; }
  }
</style>
