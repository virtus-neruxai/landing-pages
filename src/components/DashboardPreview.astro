<section class="section dashboard-section" id="dashboard" aria-labelledby="dashboard-title" data-track-section="dashboard">
  <div class="container">
    <div class="dashboard-heading">
      <div>
        <span class="section-kicker">Tu progreso, sin ruido</span>
        <h2 class="section-title" id="dashboard-title">Todo tu progreso, en un solo sitio</h2>
      </div>
      <p class="section-lede">Métricas reales de tu avance, y desafíos que se convierten en rutina para seguir creciendo.</p>
    </div>

    <div class="dashboard-stage">
      <div class="dashboard-mockup" role="img" aria-label="Vista ilustrativa del dashboard de Virtus">
        <div class="mock-sidebar" aria-hidden="true">
          <div class="mock-brand">
            <span></span>
            <strong>Virtus</strong>
          </div>
          <div class="mock-nav">
            <i class="active"></i><i></i><i></i><i></i><i></i>
          </div>
          <div class="mock-avatar"></div>
        </div>

        <div class="mock-main">
          <div class="mock-header">
            <div>
              <span class="mock-eyebrow">MARTES · 21 JULIO</span>
              <h3>Tu brújula de hoy</h3>
            </div>
            <div class="level-chip"><span>NIVEL</span><strong>12</strong></div>
          </div>

          <div class="mock-metrics">
            <div>
              <span>PROGRESO SEMANAL</span>
              <strong>68%</strong>
              <div class="metric-bar"><i></i></div>
            </div>
            <div>
              <span>ENERGÍA MEDIA</span>
              <strong>3.6 <small>/ 5</small></strong>
              <div class="metric-spark" aria-hidden="true">
                <i></i><i></i><i></i><i></i><i></i><i></i><i></i>
              </div>
            </div>
          </div>

          <div class="mock-content-grid">
            <div class="task-panel">
              <div class="panel-heading">
                <div><span class="panel-dot"></span><strong>Siguientes pasos</strong></div>
                <span>3 HOY</span>
              </div>
              <ul>
                <li class="complete"><span class="mock-check">✓</span><div><strong>Definir el entregable mínimo</strong><small>Proyecto final · 15 min</small></div><time>09:20</time></li>
                <li><span class="mock-check"></span><div><strong>Primer bloque sin distracciones</strong><small>Rutina de foco · 25 min</small></div><time>17:00</time></li>
                <li><span class="mock-check"></span><div><strong>Registrar cómo llegas</strong><small>Check-in · 2 min</small></div><time>21:30</time></li>
              </ul>
            </div>

            <div class="mission-panel">
              <div class="mission-icon" aria-hidden="true">↗</div>
              <span>MISIÓN ACTIVA</span>
              <h4>Terminar la carrera sin perderme por el camino</h4>
              <div class="mission-progress"><i></i></div>
              <div class="mission-meta"><span>Semana 3 de 8</span><strong>42%</strong></div>
            </div>
          </div>

          <div class="challenge-strip">
            <span class="challenge-label">DESAFÍO EN CURSO</span>
            <strong>7 días empezando por la tarea más pequeña</strong>
            <span class="challenge-days"><i class="done"></i><i class="done"></i><i class="done"></i><i class="today"></i><i></i><i></i><i></i></span>
          </div>
        </div>
      </div>

      <div class="annotation annotation--tasks"><span>01</span> Tus siguientes pasos, sin una lista infinita</div>
      <div class="annotation annotation--mission"><span>02</span> Una misión que da sentido a cada tarea</div>
      <div class="annotation annotation--context"><span>03</span> Avance medido con tu contexto real</div>
      <p class="dashboard-caption">Vista ilustrativa del producto · contenido de ejemplo</p>
    </div>
  </div>
</section>

<style>
  .dashboard-section {
    overflow: hidden;
    background:
      radial-gradient(ellipse at 100% 20%, color-mix(in srgb, var(--flow) 8%, transparent), transparent 28rem),
      var(--background);
  }

  .dashboard-heading {
    display: grid;
    gap: 1rem 4rem;
    align-items: end;
    margin-bottom: 4.5rem;
  }

  .dashboard-heading .section-title,
  .dashboard-heading .section-lede {
    margin-bottom: 0;
  }

  .dashboard-stage {
    position: relative;
    padding-bottom: 8rem;
  }

  .dashboard-mockup {
    display: grid;
    min-height: 34rem;
    overflow: hidden;
    border: 1px solid var(--line-strong);
    border-radius: var(--radius-lg);
    background: var(--surface-strong);
    box-shadow: var(--shadow-lg);
  }

  .mock-sidebar {
    display: none;
    flex-direction: column;
    align-items: center;
    justify-content: space-between;
    padding: 1.2rem 0.75rem;
    border-right: 1px solid color-mix(in srgb, var(--root) 18%, var(--line));
    background: color-mix(in srgb, var(--sage) 16%, var(--surface));
    color: var(--root);
  }

  .mock-brand {
    display: grid;
    justify-items: center;
    gap: 0.35rem;
  }

  .mock-brand span {
    width: 1.45rem;
    height: 1.45rem;
    border-radius: 68% 32% 58% 42% / 42% 64% 36% 58%;
    background: var(--gradient-brand);
    transform: rotate(-12deg);
  }

  .mock-brand strong {
    font-size: 0.54rem;
    color: var(--root);
    font-family: var(--font-body);
  }

  .mock-nav {
    display: grid;
    gap: 1.15rem;
  }

  .mock-nav i {
    width: 1.25rem;
    height: 1.25rem;
    border: 1px solid color-mix(in srgb, var(--root) 20%, var(--line));
    border-radius: 0.3rem;
  }

  .mock-nav i.active {
    border-color: var(--flow);
    background: color-mix(in srgb, var(--flow) 14%, var(--surface));
  }

  .mock-avatar {
    width: 1.8rem;
    height: 1.8rem;
    border-radius: 50%;
    background: var(--gradient-brand);
  }

  .mock-main {
    min-width: 0;
    padding: clamp(1rem, 3vw, 2.2rem);
  }

  .mock-header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .mock-eyebrow,
  .mock-metrics span,
  .panel-heading,
  .mission-panel > span,
  .challenge-label {
    color: var(--text-muted);
    font-family: var(--font-body);
    font-size: 0.53rem;
    font-weight: 650;
  }

  .mock-header h3 {
    margin: 0.2rem 0 0;
    font-family: var(--font-display);
    font-size: clamp(1.55rem, 4vw, 2.2rem);
    font-weight: 500;
    font-style: italic;
  }

  .level-chip {
    display: grid;
    grid-template-columns: auto auto;
    gap: 0.5rem;
    align-items: center;
    border: 1px solid color-mix(in srgb, var(--root) 22%, var(--line));
    border-radius: 999px;
    padding: 0.35rem 0.65rem;
    background: color-mix(in srgb, var(--sage) 13%, var(--surface));
    color: var(--root);
    font-family: var(--font-body);
    font-variant-numeric: tabular-nums;
  }

  .level-chip span { font-size: 0.5rem; }
  .level-chip strong { font-size: 0.73rem; }

  .mock-metrics {
    display: grid;
    gap: 0.7rem;
    margin-bottom: 0.7rem;
  }

  .mock-metrics > div {
    min-height: 6.8rem;
    border: 1px solid var(--line);
    border-radius: var(--radius-sm);
    padding: 1rem;
    background: var(--surface);
  }

  .mock-metrics strong {
    display: block;
    margin-top: 0.25rem;
    color: var(--text);
    font-family: var(--font-body);
    font-variant-numeric: tabular-nums;
    font-size: 1.35rem;
  }

  .mock-metrics strong small { color: var(--text-muted); font-size: 0.6rem; }

  .metric-bar {
    height: 0.35rem;
    margin-top: 0.65rem;
    overflow: hidden;
    border-radius: 1rem;
    background: color-mix(in srgb, var(--flow) 11%, var(--surface-strong));
  }

  .metric-bar i {
    display: block;
    width: 68%;
    height: 100%;
    background: var(--gradient-brand);
  }

  .metric-spark {
    display: flex;
    height: 1.6rem;
    align-items: end;
    gap: 0.24rem;
    margin-top: 0.4rem;
  }

  .metric-spark i {
    width: 0.35rem;
    border-radius: 0.15rem 0.15rem 0 0;
    background: var(--sage);
  }

  .metric-spark i:nth-child(1) { height: 35%; }
  .metric-spark i:nth-child(2) { height: 55%; }
  .metric-spark i:nth-child(3) { height: 44%; }
  .metric-spark i:nth-child(4) { height: 72%; }
  .metric-spark i:nth-child(5) { height: 63%; }
  .metric-spark i:nth-child(6) { height: 78%; }
  .metric-spark i:nth-child(7) { height: 58%; }

  .mock-content-grid {
    display: grid;
    gap: 0.7rem;
  }

  .task-panel,
  .mission-panel,
  .challenge-strip {
    border: 1px solid var(--line);
    border-radius: var(--radius-sm);
    background: var(--surface);
  }

  .task-panel {
    padding: 1rem;
  }

  .panel-heading {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-bottom: 0.65rem;
  }

  .panel-heading > div {
    display: flex;
    align-items: center;
    gap: 0.4rem;
  }

  .panel-heading strong {
    color: var(--text);
    font-family: var(--font-body);
    font-size: 0.7rem;
  }

  .panel-dot {
    width: 0.4rem;
    height: 0.4rem;
    border-radius: 50%;
    background: var(--flow);
  }

  .task-panel ul {
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .task-panel li {
    display: grid;
    grid-template-columns: auto 1fr auto;
    gap: 0.55rem;
    align-items: center;
    border-top: 1px solid var(--line);
    padding: 0.62rem 0;
  }

  .mock-check {
    display: grid;
    width: 1rem;
    height: 1rem;
    place-items: center;
    border: 1px solid var(--line-strong);
    border-radius: 50%;
    font-size: 0.55rem;
  }

  li.complete .mock-check {
    border-color: var(--flow);
    background: var(--gradient-brand);
    color: var(--surface);
  }

  .task-panel li strong,
  .task-panel li small {
    display: block;
  }

  .task-panel li strong { font-size: 0.62rem; }
  .task-panel li small { color: var(--text-muted); font-size: 0.52rem; }
  .task-panel time { color: var(--text-muted); font-family: var(--font-body); font-size: 0.48rem; font-variant-numeric: tabular-nums; }

  .mission-panel {
    position: relative;
    overflow: hidden;
    padding: 1rem;
    background: var(--root);
    color: var(--surface);
  }

  .mission-panel::after {
    position: absolute;
    right: -2.6rem;
    bottom: -3.2rem;
    width: 8rem;
    height: 7rem;
    border-radius: 67% 33% 54% 46% / 42% 62% 38% 58%;
    background: color-mix(in srgb, var(--sage) 18%, transparent);
    content: '';
    transform: rotate(17deg);
  }

  .mission-icon {
    display: grid;
    width: 2rem;
    height: 2rem;
    place-items: center;
    margin-bottom: 1rem;
    border: 1px solid color-mix(in srgb, var(--surface) 28%, transparent);
    border-radius: 65% 35% 58% 42% / 38% 62% 38% 62%;
    background: color-mix(in srgb, var(--sage) 20%, transparent);
    color: var(--surface);
  }

  .mission-panel > span { color: color-mix(in srgb, var(--surface) 86%, transparent); }

  .mission-panel h4 {
    max-width: 17rem;
    margin: 0.4rem 0 1.3rem;
    font-family: var(--font-display);
    font-size: 1.15rem;
    font-weight: 500;
    font-style: italic;
    line-height: 1.2;
  }

  .mission-progress {
    height: 0.28rem;
    overflow: hidden;
    border-radius: 1rem;
    background: color-mix(in srgb, var(--surface) 18%, transparent);
  }

  .mission-progress i { display: block; width: 42%; height: 100%; background: var(--sage); }

  .mission-meta {
    display: flex;
    justify-content: space-between;
    margin-top: 0.45rem;
    color: color-mix(in srgb, var(--surface) 86%, transparent);
    font-family: var(--font-body);
    font-variant-numeric: tabular-nums;
    font-size: 0.48rem;
  }

  .mission-meta strong { color: var(--surface); }

  .challenge-strip {
    display: none;
    grid-template-columns: auto 1fr auto;
    gap: 1rem;
    align-items: center;
    margin-top: 0.7rem;
    padding: 0.85rem 1rem;
  }

  .challenge-strip > strong { font-size: 0.65rem; }

  .challenge-days {
    display: flex;
    gap: 0.27rem;
  }

  .challenge-days i {
    width: 0.48rem;
    height: 0.48rem;
    border: 1px solid var(--line);
    border-radius: 50%;
  }

  .challenge-days i.done { border-color: var(--root); background: var(--root); }
  .challenge-days i.today { border: 2px solid var(--flow); }

  .annotation {
    position: absolute;
    display: none;
    max-width: 14rem;
    align-items: center;
    gap: 0.6rem;
    color: var(--text-muted);
    font-family: var(--font-body);
    font-size: 0.6rem;
    line-height: 1.45;
  }

  .annotation span {
    display: grid;
    width: 1.65rem;
    height: 1.65rem;
    flex: 0 0 auto;
    place-items: center;
    border-radius: 60% 40% 55% 45% / 43% 60% 40% 57%;
    background: var(--gradient-brand);
    color: var(--surface);
    font-size: 0.54rem;
  }

  .dashboard-caption {
    position: absolute;
    right: 0;
    bottom: 5.2rem;
    margin: 0;
    color: var(--text-muted);
    font-size: 0.6rem;
  }

  @media (min-width: 35rem) {
    .mock-metrics {
      grid-template-columns: 1fr 1fr;
    }

    .challenge-strip {
      display: grid;
    }
  }

  @media (min-width: 54rem) {
    .dashboard-heading {
      grid-template-columns: minmax(0, 1fr) minmax(20rem, 0.7fr);
    }

    .dashboard-mockup {
      grid-template-columns: 5rem minmax(0, 1fr);
    }

    .mock-sidebar {
      display: flex;
    }

    .mock-content-grid {
      grid-template-columns: minmax(0, 1.35fr) minmax(14rem, 0.65fr);
    }
  }

  @media (min-width: 70rem) {
    .dashboard-stage {
      padding-inline: 3rem;
    }

    .annotation {
      display: flex;
    }

    .annotation--tasks { bottom: 3rem; left: 6rem; }
    .annotation--mission { right: 5rem; bottom: 3rem; }
    .annotation--context { top: -2.8rem; right: 4rem; }
    .dashboard-caption { right: 3rem; }
  }
</style>
