<section class="section early" id="primeros" aria-labelledby="early-title" data-track-section="early_access">
  <div class="container early-panel">
    <div class="early-mark" aria-hidden="true">
      <svg viewBox="0 0 84 84" fill="none">
        <path d="M18 52c15-2 23-12 25-30 14 8 20 19 14 31-7 13-24 14-39-1Z" fill="currentColor" opacity=".18"></path>
        <path d="M22 57c10-12 21-20 34-24" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"></path>
        <path d="M27 48c7 0 12 3 15 8M37 39c6 0 10 2 13 6" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" opacity=".62"></path>
      </svg>
    </div>

    <div class="early-copy">
      <span class="section-kicker">Primera etapa</span>
      <h2 class="section-title" id="early-title">Únete a los primeros en probar Virtus</h2>
      <p>Sin cifras infladas ni promesas prestadas. Estamos abriendo el camino con personas que quieren dejar de avanzar por inercia y empezar a moverse con dirección.</p>
    </div>

    <div class="early-action">
      <a class="button button--primary" href="#prueba-tu-mision" data-focus-demo data-track="mission_cta_clicked" data-track-location="early_access">
        Escribe tu misión
        <span aria-hidden="true">→</span>
      </a>
      <small>Prueba el resultado antes de crear una cuenta.</small>
    </div>
  </div>
</section>

<style>
  .early {
    background: var(--background);
  }

  .early-panel {
    position: relative;
    display: grid;
    gap: 2rem;
    align-items: center;
    overflow: hidden;
    border: 1px solid color-mix(in srgb, var(--sage) 32%, var(--line));
    border-radius: var(--radius-lg);
    padding: clamp(1.6rem, 5vw, 3.5rem);
    background:
      linear-gradient(125deg, color-mix(in srgb, var(--flow) 7%, var(--surface)), color-mix(in srgb, var(--sage) 13%, var(--surface)));
    box-shadow: var(--shadow-sm);
  }

  .early-panel::after {
    position: absolute;
    right: -7rem;
    bottom: -7rem;
    width: 19rem;
    height: 15rem;
    border-radius: 66% 34% 57% 43% / 43% 61% 39% 57%;
    background: color-mix(in srgb, var(--root) 10%, transparent);
    content: '';
    transform: rotate(-13deg);
  }

  .early-mark {
    width: 4.6rem;
    color: var(--root);
  }

  .early .section-kicker {
    color: var(--root);
  }

  .early .section-title {
    margin-bottom: 0.8rem;
  }

  .early-copy p {
    max-width: 43rem;
    margin: 0;
    color: var(--text-muted);
  }

  .early-action {
    position: relative;
    z-index: 1;
    display: flex;
    align-items: flex-start;
    flex-direction: column;
    gap: 0.65rem;
  }

  .early-action small {
    color: var(--text-muted);
    font-size: 0.67rem;
  }

  @media (min-width: 64rem) {
    .early-panel {
      grid-template-columns: auto minmax(0, 1fr) auto;
      gap: 2.5rem;
    }

    .early-action {
      align-items: center;
    }
  }
</style>
