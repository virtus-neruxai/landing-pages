<section class="section difference" id="diferencia" aria-labelledby="difference-title" data-track-section="differentiation">
  <div class="container difference-grid">
    <div class="difference-content">
      <span class="section-kicker">La diferencia</span>
      <h2 class="section-title" id="difference-title">No es otra lista de tareas gamificada</h2>
      <p class="section-lede">Virtus no premia que repitas por repetir. Entiende lo que pasa alrededor de cada tarea para ayudarte a avanzar con sentido.</p>

      <table class="comparison" aria-label="Comparación entre las apps habituales y Virtus">
        <thead>
          <tr>
            <th scope="col">Lo habitual</th>
            <th scope="col">Virtus</th>
          </tr>
        </thead>
        <tbody>
        <tr>
          <td>
            <span class="cell-label">Lo habitual</span>
            Repite la misma tarea aunque llevas 3 días evitándola
          </td>
          <td>
            <span class="cell-label">Virtus</span>
            Nota el patrón y ajusta el plan contigo
          </td>
        </tr>
        <tr>
          <td>
            <span class="cell-label">Lo habitual</span>
            Rachas y puntos sin contexto
          </td>
          <td>
            <span class="cell-label">Virtus</span>
            Un resumen que dice qué está funcionando de verdad — y por qué
          </td>
        </tr>
        </tbody>
      </table>
    </div>

    <aside class="insight-card" aria-label="Ejemplo ilustrativo de un insight semanal de Virtus">
      <div class="insight-topbar">
        <div>
          <span class="insight-dot"></span>
          <span>RESUMEN SEMANAL</span>
        </div>
        <span>SEM · 08</span>
      </div>

      <div class="insight-mark" aria-hidden="true">
        <svg viewBox="0 0 110 110" fill="none">
          <path d="M20 75c20-3 28-24 33-48 18 12 28 28 23 44-6 17-27 22-56 4Z" fill="currentColor" opacity=".13"></path>
          <path d="M23 78c18-12 31-28 39-49M39 61c8 0 16 3 23 9M47 49c-1-7 0-13 2-19" stroke="currentColor" stroke-width="3" stroke-linecap="round"></path>
        </svg>
      </div>

      <p class="insight-label">LO QUE ESTÁ FUNCIONANDO</p>
      <h3>Avanzas más cuando el primer paso cabe en 25 minutos.</h3>
      <p class="insight-body">Esta semana completaste 4 de 5 bloques cortos. Las sesiones largas se quedaron a medias.</p>
      <div class="insight-action">
        <span>Próximo ajuste</span>
        Dividimos el trabajo de mañana en dos bloques breves.
      </div>
      <p class="mockup-caption">Ejemplo ilustrativo de un resumen</p>
    </aside>
  </div>
</section>

<style>
  .difference {
    background:
      radial-gradient(ellipse at 10% 88%, color-mix(in srgb, var(--root) 11%, transparent), transparent 36%),
      radial-gradient(ellipse at 92% 8%, color-mix(in srgb, var(--flow) 10%, transparent), transparent 38%),
      var(--background);
    color: var(--text);
  }

  .difference-grid {
    display: grid;
    gap: 4rem;
    align-items: center;
  }

  .difference .section-kicker {
    color: var(--root);
  }

  .difference .section-title {
    max-width: 15ch;
  }

  .difference .section-lede {
    color: var(--text-muted);
  }

  .comparison {
    display: block;
    width: 100%;
    margin-top: 2.5rem;
    border-top: 1px solid var(--line-strong);
    border-collapse: collapse;
  }

  .comparison thead {
    display: none;
  }

  .comparison tbody,
  .comparison tr,
  .comparison td {
    display: block;
  }

  .comparison th {
    padding-block: 0.75rem;
    color: var(--text-muted);
    font-family: var(--font-body);
    font-size: 0.68rem;
    font-weight: 750;
    text-align: left;
  }

  .comparison th:last-child {
    padding-left: 1.5rem;
    color: var(--root);
  }

  .comparison tr {
    display: grid;
    border-bottom: 1px solid var(--line);
  }

  .comparison td {
    padding: 1.25rem 0;
    color: var(--text-muted);
    font-size: 0.94rem;
    line-height: 1.5;
  }

  .comparison td:last-child {
    border-top: 1px solid var(--line);
    color: var(--text);
    font-weight: 620;
  }

  .cell-label {
    display: block;
    margin-bottom: 0.35rem;
    color: var(--text-muted);
    font-family: var(--font-body);
    font-size: 0.62rem;
    font-weight: 700;
  }

  .comparison td:last-child .cell-label {
    color: var(--root);
  }

  .insight-card {
    position: relative;
    max-width: 32rem;
    overflow: hidden;
    border: 1px solid color-mix(in srgb, var(--flow) 16%, var(--line));
    border-radius: var(--radius-lg) var(--radius-lg) var(--radius-sm) var(--radius-lg);
    padding: clamp(1.4rem, 4vw, 2.2rem);
    background:
      linear-gradient(145deg, color-mix(in srgb, var(--surface) 90%, var(--flow) 10%), color-mix(in srgb, var(--surface) 94%, var(--root) 6%));
    color: var(--text);
    box-shadow: var(--shadow-lg);
  }

  .insight-card::after {
    position: absolute;
    right: -5rem;
    bottom: -5rem;
    width: 15rem;
    height: 15rem;
    border-radius: 63% 37% 31% 69% / 46% 58% 42% 54%;
    background: color-mix(in srgb, var(--sage) 12%, transparent);
    content: '';
  }

  .insight-topbar {
    position: relative;
    z-index: 2;
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 2rem;
    color: var(--text-muted);
    font-family: var(--font-body);
    font-size: 0.62rem;
    font-weight: 700;
  }

  .insight-topbar > div {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .insight-dot {
    width: 0.45rem;
    height: 0.45rem;
    border-radius: 50%;
    background: var(--root);
  }

  .insight-mark {
    position: absolute;
    top: 3.2rem;
    right: 1.5rem;
    width: 6.8rem;
    color: var(--root);
    opacity: 0.72;
  }

  .insight-label {
    position: relative;
    z-index: 2;
    margin-bottom: 0.75rem;
    color: var(--flow-deep);
    font-family: var(--font-body);
    font-size: 0.64rem;
    font-weight: 750;
  }

  .insight-card h3 {
    position: relative;
    z-index: 2;
    max-width: 16ch;
    margin-bottom: 1rem;
    font-family: var(--font-display);
    font-size: clamp(1.6rem, 5vw, 2.3rem);
    font-style: italic;
    font-weight: 500;
    line-height: 1.12;
  }

  .insight-body {
    position: relative;
    z-index: 2;
    max-width: 28rem;
    margin-bottom: 1.5rem;
    color: var(--text-muted);
    font-size: 0.86rem;
  }

  .insight-action {
    position: relative;
    z-index: 2;
    border-left: 3px solid var(--root);
    padding: 0.85rem 1rem;
    background: color-mix(in srgb, var(--sage) 15%, var(--surface));
    font-size: 0.78rem;
    line-height: 1.45;
  }

  .insight-action span {
    display: block;
    margin-bottom: 0.25rem;
    color: var(--root);
    font-family: var(--font-body);
    font-size: 0.62rem;
    font-weight: 750;
  }

  .mockup-caption {
    position: relative;
    z-index: 2;
    margin: 1rem 0 0;
    color: var(--text-muted);
    font-size: 0.62rem;
    text-align: right;
  }

  @media (min-width: 42rem) {
    .comparison {
      display: table;
    }

    .comparison thead {
      display: table-header-group;
    }

    .comparison tbody {
      display: table-row-group;
    }

    .comparison tr {
      display: table-row;
    }

    .comparison td {
      display: table-cell;
      width: 50%;
      border-bottom: 1px solid var(--line);
      padding: 1.25rem 1.5rem 1.25rem 0;
    }

    .comparison td:last-child {
      border-top: 0;
      border-left: 1px solid var(--line);
      padding-left: 1.5rem;
    }

    .cell-label {
      display: none;
    }
  }

  @media (min-width: 62rem) {
    .difference-grid {
      grid-template-columns: minmax(0, 1.08fr) minmax(27rem, 0.92fr);
      gap: clamp(4rem, 8vw, 8rem);
    }

    .insight-card {
      justify-self: end;
    }
  }
</style>
